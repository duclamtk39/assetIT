import { useEffect, useMemo, useState } from 'react'
import { AlertTriangle, BarChart3, Check, ChevronRight, ClipboardCheck, FileCheck2, Plus, Search, ShieldAlert, ShieldCheck, Target, UserRound, X } from 'lucide-react'
import type { Asset } from '../../types'
import { api, ApiError } from '../../services/api-client'

type Level = 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL'
type RiskStatus = 'IDENTIFIED' | 'ASSESSED' | 'TREATMENT_PLANNED' | 'TREATING' | 'MONITORING' | 'ACCEPTED' | 'CLOSED'
type AssessmentStatus = 'DRAFT' | 'IN_REVIEW' | 'APPROVED' | 'TREATMENT' | 'MONITORING' | 'CLOSED' | 'CANCELLED'
type TreatmentStrategy = 'AVOID' | 'MITIGATE' | 'TRANSFER' | 'ACCEPT'
interface Lookup { id: string; name?: string; fullName?: string; email?: string; role?: string; department?: { id: string; name: string } }
interface Assessment { id: string; assessmentNo: string; title: string; scope: string; methodology: string; status: AssessmentStatus; startDate: string; targetDate?: string; nextReviewAt?: string; owner: Lookup; approver?: Lookup; department?: Lookup; _count?: { risks: number } }
interface Control { id: string; controlCode?: string; title: string; framework?: string; status: string; effectiveness?: number; evidence?: string }
interface Treatment { id: string; title: string; description?: string; status: string; progress: number; dueDate: string; outcome?: string; assignee: Lookup }
interface Risk {
  id: string; riskNo: string; title: string; category: string; scenario: string; threat: string; vulnerability: string; existingControls?: string; status: RiskStatus
  likelihood: number; impact: number; inherentScore: number; inherentLevel: Level; residualLikelihood?: number; residualImpact?: number; residualScore?: number; residualLevel?: Level
  treatmentStrategy: TreatmentStrategy; acceptanceRationale?: string; dueDate?: string; nextReviewAt?: string; owner: Lookup; department?: Lookup; assessment: Assessment
  assets: Array<{ asset: { id: string; assetTag: string; name: string; serialNumber?: string } }>; incidents: Array<{ incident: { id: string; incidentNo: string; title: string; status: string } }>; controls: Control[]; treatments: Treatment[]
}
interface Summary { totalOpen: number; critical: number; high: number; overdue: number; reviewDue: number; treatments: number; byCategory: Array<{ label: string; count: number }>; byLevel: Array<{ label: string; count: number }>; matrix: Array<{ impact: number; likelihood: number; count: number }> }

const levelLabels: Record<Level, string> = { LOW: 'Thấp', MEDIUM: 'Trung bình', HIGH: 'Cao', CRITICAL: 'Nghiêm trọng' }
const statusLabels: Record<RiskStatus, string> = { IDENTIFIED: 'Đã nhận diện', ASSESSED: 'Đã đánh giá', TREATMENT_PLANNED: 'Đã lập kế hoạch', TREATING: 'Đang xử lý', MONITORING: 'Đang theo dõi', ACCEPTED: 'Đã chấp nhận', CLOSED: 'Đã đóng' }
const assessmentLabels: Record<AssessmentStatus, string> = { DRAFT: 'Bản nháp', IN_REVIEW: 'Chờ phê duyệt', APPROVED: 'Đã phê duyệt', TREATMENT: 'Đang xử lý', MONITORING: 'Đang theo dõi', CLOSED: 'Đã đóng', CANCELLED: 'Đã hủy' }
const strategyLabels: Record<TreatmentStrategy, string> = { AVOID: 'Tránh', MITIGATE: 'Giảm thiểu', TRANSFER: 'Chuyển giao', ACCEPT: 'Chấp nhận' }
const emptySummary: Summary = { totalOpen: 0, critical: 0, high: 0, overdue: 0, reviewDue: 0, treatments: 0, byCategory: [], byLevel: [], matrix: Array.from({ length: 25 }, (_, i) => ({ impact: 5 - Math.floor(i / 5), likelihood: i % 5 + 1, count: 0 })) }
const apiMessage = (error: unknown) => error instanceof ApiError ? error.message : 'Không thể kết nối dịch vụ đánh giá rủi ro.'
const date = (value?: string) => value ? new Date(value).toLocaleDateString('vi-VN') : '—'
const scoreLevel = (score: number): Level => score >= 17 ? 'CRITICAL' : score >= 10 ? 'HIGH' : score >= 5 ? 'MEDIUM' : 'LOW'

const demoAssessment: Assessment = { id: 'demo-assessment', assessmentNo: 'DGRR-2026-0001', title: 'Đánh giá rủi ro hạ tầng CNTT 2026', scope: 'Thiết bị đầu cuối, hạ tầng mạng, phòng máy chủ và dịch vụ đám mây.', methodology: 'ISO 27005 / NIST SP 800-30', status: 'APPROVED', startDate: '2026-08-01', targetDate: '2026-09-15', nextReviewAt: '2026-11-01', owner: { id: 'it-1', fullName: 'Trần Đức Long' }, approver: { id: 'admin-1', fullName: 'Quản trị viên' }, department: { id: 'd-it', name: 'IT' }, _count: { risks: 3 } }
const demoRisks: Risk[] = [
  { id: 'r1', riskNo: 'RR-2026-0001', title: 'Gián đoạn kết nối Internet tại văn phòng chính', category: 'Hạ tầng mạng', scenario: 'Đường truyền chính và thiết bị biên đồng thời gặp sự cố.', threat: 'Lỗi nhà mạng hoặc thiết bị biên', vulnerability: 'Chưa kiểm thử chuyển mạch dự phòng định kỳ', existingControls: 'Hai đường truyền từ hai ISP, firewall HA', status: 'TREATING', likelihood: 4, impact: 5, inherentScore: 20, inherentLevel: 'CRITICAL', residualLikelihood: 2, residualImpact: 4, residualScore: 8, residualLevel: 'MEDIUM', treatmentStrategy: 'MITIGATE', dueDate: '2026-09-05', nextReviewAt: '2026-10-01', owner: { id: 'it-1', fullName: 'Trần Đức Long' }, department: { id: 'd-it', name: 'IT' }, assessment: demoAssessment, assets: [], incidents: [], controls: [{ id: 'c1', controlCode: 'A.8.14', title: 'Dự phòng cơ sở xử lý thông tin', framework: 'ISO/IEC 27001', status: 'PARTIAL', effectiveness: 60 }], treatments: [{ id: 't1', title: 'Diễn tập failover hai đường truyền', status: 'IN_PROGRESS', progress: 60, dueDate: '2026-09-05', assignee: { id: 'it-1', fullName: 'Trần Đức Long' } }] },
  { id: 'r2', riskNo: 'RR-2026-0002', title: 'Mã độc mã hóa dữ liệu máy người dùng', category: 'An toàn thông tin', scenario: 'Người dùng mở tệp đính kèm độc hại, mã độc lan qua thư mục dùng chung.', threat: 'Phishing và ransomware', vulnerability: 'Nhận thức người dùng và phân quyền thư mục chưa đồng đều', existingControls: 'EDR, email filtering, backup hằng ngày', status: 'ASSESSED', likelihood: 3, impact: 5, inherentScore: 15, inherentLevel: 'HIGH', residualLikelihood: 2, residualImpact: 4, residualScore: 8, residualLevel: 'MEDIUM', treatmentStrategy: 'MITIGATE', dueDate: '2026-09-20', owner: { id: 'it-1', fullName: 'Trần Đức Long' }, department: { id: 'd-it', name: 'IT' }, assessment: demoAssessment, assets: [], incidents: [], controls: [], treatments: [] },
  { id: 'r3', riskNo: 'RR-2026-0003', title: 'Hết hạn chứng thư SSL dịch vụ khách hàng', category: 'Dịch vụ số', scenario: 'Chứng thư hết hạn làm trình duyệt từ chối kết nối.', threat: 'Bỏ sót lịch gia hạn', vulnerability: 'Phụ thuộc theo dõi thủ công', existingControls: 'Cảnh báo gia hạn AssetFlow', status: 'MONITORING', likelihood: 2, impact: 4, inherentScore: 8, inherentLevel: 'MEDIUM', residualLikelihood: 1, residualImpact: 3, residualScore: 3, residualLevel: 'LOW', treatmentStrategy: 'MITIGATE', nextReviewAt: '2026-09-01', owner: { id: 'admin-1', fullName: 'Quản trị viên' }, department: { id: 'd-it', name: 'IT' }, assessment: demoAssessment, assets: [], incidents: [], controls: [], treatments: [] },
]

function summarize(items: Risk[]): Summary {
  const open = items.filter(item => !['ACCEPTED', 'CLOSED'].includes(item.status)), now = new Date()
  const by = (values: string[]) => Object.entries(values.reduce((acc, value) => { acc[value] = (acc[value] || 0) + 1; return acc }, {} as Record<string, number>)).map(([label, count]) => ({ label, count })).sort((a, b) => b.count - a.count)
  return { totalOpen: open.length, critical: open.filter(item => (item.residualLevel || item.inherentLevel) === 'CRITICAL').length, high: open.filter(item => (item.residualLevel || item.inherentLevel) === 'HIGH').length, overdue: open.filter(item => item.dueDate && new Date(item.dueDate) < now).length, reviewDue: open.filter(item => item.nextReviewAt && new Date(item.nextReviewAt) < now).length, treatments: open.filter(item => ['TREATMENT_PLANNED', 'TREATING'].includes(item.status)).length, byCategory: by(open.map(item => item.category)), byLevel: by(open.map(item => item.residualLevel || item.inherentLevel)), matrix: Array.from({ length: 25 }, (_, i) => ({ impact: 5 - Math.floor(i / 5), likelihood: i % 5 + 1, count: open.filter(item => item.impact === 5 - Math.floor(i / 5) && item.likelihood === i % 5 + 1).length })) }
}

export function RiskManagement({ assets, demoMode, currentUserName }: { assets: Asset[]; demoMode: boolean; currentUserName: string }) {
  const [summary, setSummary] = useState<Summary>(demoMode ? summarize(demoRisks) : emptySummary)
  const [risks, setRisks] = useState<Risk[]>(demoMode ? demoRisks : [])
  const [assessments, setAssessments] = useState<Assessment[]>(demoMode ? [demoAssessment] : [])
  const [operators, setOperators] = useState<Lookup[]>(demoMode ? [{ id: 'admin-1', fullName: 'Quản trị viên', role: 'ADMIN' }, { id: 'it-1', fullName: 'Trần Đức Long', role: 'IT' }] : [])
  const [departments, setDepartments] = useState<Lookup[]>(demoMode ? [{ id: 'd-it', name: 'IT' }] : [])
  const [tab, setTab] = useState<'register' | 'assessments'>('register')
  const [query, setQuery] = useState(''), [level, setLevel] = useState(''), [status, setStatus] = useState('')
  const [selected, setSelected] = useState<Risk>(), [creatingRisk, setCreatingRisk] = useState(false), [creatingAssessment, setCreatingAssessment] = useState(false)
  const [loading, setLoading] = useState(false), [error, setError] = useState('')

  const load = async () => {
    if (demoMode) return
    setLoading(true); setError('')
    try {
      const params = new URLSearchParams({ page: '1', limit: '100' }); if (query) params.set('search', query); if (level) params.set('level', level); if (status) params.set('status', status)
      const [stats, riskResult, assessmentResult] = await Promise.all([api.get<Summary>('/risk-assessments/summary'), api.get<{ data: Risk[] }>(`/risk-assessments/risks?${params}`), api.get<{ data: Assessment[] }>('/risk-assessments?limit=100')])
      setSummary(stats); setRisks(riskResult.data); setAssessments(assessmentResult.data)
    } catch (reason) { setError(apiMessage(reason)) } finally { setLoading(false) }
  }
  useEffect(() => { const timer = setTimeout(() => void load(), query ? 250 : 0); return () => clearTimeout(timer) }, [demoMode, query, level, status])
  useEffect(() => { if (demoMode) return; void Promise.all([api.get<Lookup[]>('/risk-assessments/operators'), api.get<Lookup[]>('/departments')]).then(([users, deps]) => { setOperators(users); setDepartments(deps) }).catch(() => undefined) }, [demoMode])
  const shown = useMemo(() => demoMode ? risks.filter(item => (!query || `${item.riskNo} ${item.title} ${item.category} ${item.threat} ${item.vulnerability}`.toLowerCase().includes(query.toLowerCase())) && (!level || (item.residualLevel || item.inherentLevel) === level) && (!status || item.status === status)) : risks, [demoMode, risks, query, level, status])

  const openDetail = async (item: Risk) => { if (demoMode) return setSelected(item); try { setSelected(await api.get<Risk>(`/risk-assessments/risks/${item.id}`)) } catch (reason) { setError(apiMessage(reason)) } }
  const saveAssessment = async (body: any) => { if (demoMode) { const created = { ...body, id: `assessment-${Date.now()}`, assessmentNo: `DGRR-DEMO-${assessments.length + 1}`, status: 'DRAFT', owner: operators.find(x => x.id === body.ownerId)!, approver: operators.find(x => x.id === body.approverId), department: departments.find(x => x.id === body.departmentId), _count: { risks: 0 } } as Assessment; setAssessments(items => [created, ...items]); setCreatingAssessment(false); return } await api.post('/risk-assessments', body); setCreatingAssessment(false); await load() }
  const saveRisk = async (body: any) => { if (demoMode) { const inherentScore = body.likelihood * body.impact, residualScore = body.residualLikelihood && body.residualImpact ? body.residualLikelihood * body.residualImpact : undefined, assessment = assessments.find(x => x.id === body.assessmentId)!; const created: Risk = { ...body, id: `risk-${Date.now()}`, riskNo: `RR-DEMO-${risks.length + 1}`, status: 'IDENTIFIED', inherentScore, inherentLevel: scoreLevel(inherentScore), residualScore, residualLevel: residualScore ? scoreLevel(residualScore) : undefined, owner: operators.find(x => x.id === body.ownerId)!, department: departments.find(x => x.id === body.departmentId), assessment, assets: [], incidents: [], controls: [], treatments: [] }; setRisks(items => [created, ...items]); setSummary(summarize([created, ...risks])); setCreatingRisk(false); return } const { assessmentId, ...payload } = body; await api.post(`/risk-assessments/${assessmentId}/risks`, payload); setCreatingRisk(false); await load() }
  const addControl = async (riskId: string, body: any) => { if (demoMode) { setSelected(current => current ? { ...current, controls: [...current.controls, { ...body, id: `control-${Date.now()}` }] } : current); return } await api.post(`/risk-assessments/risks/${riskId}/controls`, body); setSelected(await api.get<Risk>(`/risk-assessments/risks/${riskId}`)); await load() }
  const addTreatment = async (riskId: string, body: any) => { if (demoMode) { setSelected(current => current ? { ...current, status: 'TREATMENT_PLANNED', treatments: [...current.treatments, { ...body, id: `treatment-${Date.now()}`, status: 'PLANNED', progress: 0, assignee: operators.find(x => x.id === body.assigneeId)! }] } : current); return } await api.post(`/risk-assessments/risks/${riskId}/treatments`, body); setSelected(await api.get<Risk>(`/risk-assessments/risks/${riskId}`)); await load() }
  const completeTreatment = async (riskId: string, treatment: Treatment) => { const outcome = window.prompt('Kết quả thực hiện hành động xử lý:', treatment.outcome || 'Đã hoàn tất và kiểm tra hiệu lực'); if (!outcome) return; if (demoMode) { setSelected(current => current ? { ...current, treatments: current.treatments.map(item => item.id === treatment.id ? { ...item, status: 'COMPLETED', progress: 100, outcome } : item) } : current); return } await api.patch(`/risk-assessments/treatments/${treatment.id}`, { status: 'COMPLETED', progress: 100, outcome }); setSelected(await api.get<Risk>(`/risk-assessments/risks/${riskId}`)); await load() }

  const metrics = [
    { key: '', label: 'Rủi ro đang mở', value: summary.totalOpen, note: 'Cần theo dõi và xử lý', icon: ShieldAlert },
    { key: 'CRITICAL', label: 'Nghiêm trọng', value: summary.critical, note: 'Vượt ngưỡng ưu tiên', icon: AlertTriangle },
    { key: 'HIGH', label: 'Mức cao', value: summary.high, note: 'Cần kế hoạch kiểm soát', icon: Target },
    { key: 'overdue', label: 'Quá hạn xử lý', value: summary.overdue, note: 'Cần điều phối ngay', icon: ClipboardCheck },
    { key: 'review', label: 'Đến hạn rà soát', value: summary.reviewDue, note: 'Chờ đánh giá lại', icon: FileCheck2 },
  ]
  return <main className="risk-management">
    <header className="risk-heading"><div><span>GRC · ISO/IEC 27005 · NIST SP 800-30</span><h1>Đánh giá rủi ro CNTT</h1><p>Nhận diện, đánh giá, xử lý và theo dõi rủi ro gắn với tài sản, sự cố và đơn vị chịu trách nhiệm.</p></div><div><button className="btn secondary" onClick={() => setCreatingAssessment(true)}><ClipboardCheck size={16}/>Tạo đợt đánh giá</button><button className="btn primary" onClick={() => assessments.length ? setCreatingRisk(true) : setCreatingAssessment(true)}><Plus size={16}/>Ghi nhận rủi ro</button></div></header>
    {error && <div className="risk-error"><AlertTriangle size={16}/>{error}</div>}
    <section className="risk-metrics">{metrics.map((item, index) => <button key={item.label} className={(index === 1 && level === 'CRITICAL') || (index === 2 && level === 'HIGH') ? 'active' : ''} onClick={() => { if (item.key === 'CRITICAL' || item.key === 'HIGH') setLevel(current => current === item.key ? '' : item.key); else if (item.key === '') { setLevel(''); setStatus('') } }}><item.icon/><span><small>{item.label}</small><b>{item.value}</b><em>{item.note}</em></span></button>)}</section>
    <section className="risk-overview-grid">
      <article className="risk-panel risk-matrix"><header><div><h2>Ma trận rủi ro vốn có 5 × 5</h2><p>Nhấp ô để lọc danh sách theo mức rủi ro.</p></div><span>Xác suất → · Ảnh hưởng ↓</span></header><div className="matrix-body"><div className="matrix-axis">{[5,4,3,2,1].map(value => <b key={value}>{value}</b>)}</div><div className="matrix-cells">{summary.matrix.map(cell => { const cellLevel = scoreLevel(cell.impact * cell.likelihood); return <button key={`${cell.impact}-${cell.likelihood}`} className={cellLevel.toLowerCase()} onClick={() => setLevel(cellLevel)} title={`Xác suất ${cell.likelihood}, ảnh hưởng ${cell.impact}`}><b>{cell.count}</b><small>{cell.likelihood}×{cell.impact}</small></button> })}</div></div><div className="matrix-legend"><span className="low">Thấp</span><span className="medium">Trung bình</span><span className="high">Cao</span><span className="critical">Nghiêm trọng</span></div></article>
      <article className="risk-panel risk-categories"><header><div><h2>Rủi ro theo nhóm</h2><p>Phạm vi rủi ro đang mở.</p></div><BarChart3 size={18}/></header><div>{summary.byCategory.slice(0, 7).map(item => { const max = Math.max(...summary.byCategory.map(x => x.count), 1); return <button key={item.label} onClick={() => setQuery(item.label)}><span>{item.label}</span><i><em style={{ width: `${Math.max(5, item.count / max * 100)}%` }}/></i><b>{item.count}</b></button> })}{!summary.byCategory.length && <p className="risk-empty">Chưa có rủi ro trong phạm vi.</p>}</div></article>
      <article className="risk-panel risk-workflow"><header><div><h2>Quy trình kiểm soát</h2><p>Trạng thái hồ sơ và trách nhiệm.</p></div><ShieldCheck size={18}/></header><ol><li><b>1</b><span>Khoanh vùng tài sản và dịch vụ</span></li><li><b>2</b><span>Nhận diện đe dọa, điểm yếu</span></li><li><b>3</b><span>Đánh giá rủi ro vốn có</span></li><li><b>4</b><span>Kiểm soát và xử lý</span></li><li><b>5</b><span>Rủi ro còn lại, phê duyệt</span></li></ol></article>
    </section>
    <section className="risk-register">
      <header><div className="risk-tabs"><button className={tab === 'register' ? 'active' : ''} onClick={() => setTab('register')}>Sổ đăng ký rủi ro <b>{risks.length}</b></button><button className={tab === 'assessments' ? 'active' : ''} onClick={() => setTab('assessments')}>Đợt đánh giá <b>{assessments.length}</b></button></div></header>
      {tab === 'register' ? <><div className="risk-filters"><label><Search size={16}/><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Tìm mã, tiêu đề, mối đe dọa, điểm yếu hoặc tài sản..."/></label><select value={level} onChange={event => setLevel(event.target.value)}><option value="">Tất cả mức rủi ro</option>{Object.entries(levelLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select><select value={status} onChange={event => setStatus(event.target.value)}><option value="">Tất cả trạng thái</option>{Object.entries(statusLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></div><div className="risk-table-wrap"><table><thead><tr><th>MÃ RỦI RO</th><th>KỊCH BẢN RỦI RO</th><th>NHÓM / PHẠM VI</th><th>CHỦ SỞ HỮU</th><th>VỐN CÓ</th><th>CÒN LẠI</th><th>CHIẾN LƯỢC</th><th>HẠN XỬ LÝ</th><th>TRẠNG THÁI</th><th></th></tr></thead><tbody>{shown.map(item => <tr key={item.id} onClick={() => void openDetail(item)}><td><b className="risk-code">{item.riskNo}</b></td><td><b>{item.title}</b><small>{item.threat}</small></td><td>{item.category}<small>{item.department?.name || 'Toàn công ty'}</small></td><td>{item.owner.fullName}</td><td><span className={`risk-level ${item.inherentLevel.toLowerCase()}`}>{item.inherentScore} · {levelLabels[item.inherentLevel]}</span></td><td>{item.residualLevel ? <span className={`risk-level ${item.residualLevel.toLowerCase()}`}>{item.residualScore} · {levelLabels[item.residualLevel]}</span> : <span className="muted">Chưa đánh giá</span>}</td><td>{strategyLabels[item.treatmentStrategy]}</td><td className={item.dueDate && new Date(item.dueDate) < new Date() ? 'risk-overdue' : ''}>{date(item.dueDate)}</td><td><span className={`risk-status ${item.status.toLowerCase()}`}>{statusLabels[item.status]}</span></td><td><ChevronRight size={16}/></td></tr>)}</tbody></table></div>{loading && <div className="risk-loading">Đang tải sổ đăng ký rủi ro…</div>}{!loading && !shown.length && <div className="risk-empty-table">Không có rủi ro phù hợp với bộ lọc.</div>}</> : <div className="risk-table-wrap"><table><thead><tr><th>MÃ ĐỢT</th><th>TÊN / PHẠM VI</th><th>CHỦ SỞ HỮU</th><th>PHÊ DUYỆT</th><th>THỜI GIAN</th><th>SỐ RỦI RO</th><th>TRẠNG THÁI</th></tr></thead><tbody>{assessments.map(item => <tr key={item.id}><td><b className="risk-code">{item.assessmentNo}</b></td><td><b>{item.title}</b><small>{item.scope}</small></td><td>{item.owner.fullName}</td><td>{item.approver?.fullName || <span className="muted">Chưa chỉ định</span>}</td><td>{date(item.startDate)} → {date(item.targetDate)}</td><td>{item._count?.risks || 0}</td><td><span className={`assessment-status ${item.status.toLowerCase()}`}>{assessmentLabels[item.status]}</span></td></tr>)}</tbody></table></div>}
    </section>
    <section className="risk-standard-note"><ShieldCheck size={20}/><div><b>Quản trị dựa trên bằng chứng và phân tách trách nhiệm</b><p>Điểm rủi ro được tính tại API; chủ sở hữu không được tự phê duyệt hoặc tự chấp nhận rủi ro còn lại. Mọi thay đổi được ghi audit log. Việc sử dụng quy trình tham chiếu không đồng nghĩa doanh nghiệp đã được chứng nhận ISO.</p></div></section>
    {creatingAssessment && <AssessmentModal operators={operators} departments={departments} currentUserName={currentUserName} onClose={() => setCreatingAssessment(false)} onSave={saveAssessment}/>} 
    {creatingRisk && <RiskModal assessments={assessments} operators={operators} departments={departments} assets={assets} onClose={() => setCreatingRisk(false)} onSave={saveRisk}/>} 
    {selected && <RiskDetail risk={selected} operators={operators} onClose={() => setSelected(undefined)} addControl={addControl} addTreatment={addTreatment} completeTreatment={completeTreatment}/>} 
  </main>
}

function AssessmentModal({ operators, departments, onClose, onSave }: { operators: Lookup[]; departments: Lookup[]; currentUserName: string; onClose: () => void; onSave: (body: any) => Promise<void> }) {
  const [form, setForm] = useState<any>({ title: '', description: '', scope: '', methodology: 'ISO_27005_NIST_800_30', ownerId: operators[0]?.id || '', approverId: operators[1]?.id || '', departmentId: '', startDate: new Date().toISOString().slice(0, 10), targetDate: '', nextReviewAt: '' }), [error, setError] = useState(''), [saving, setSaving] = useState(false)
  const set = (key: string, value: unknown) => setForm((current: any) => ({ ...current, [key]: value }))
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(''); try { await onSave({ ...form, approverId: form.approverId || undefined, departmentId: form.departmentId || undefined, targetDate: form.targetDate || undefined, nextReviewAt: form.nextReviewAt || undefined }) } catch (reason) { setError(reason instanceof Error ? reason.message : 'Không thể tạo đợt đánh giá') } finally { setSaving(false) } }
  return <div className="modal-backdrop" onMouseDown={event => event.target === event.currentTarget && onClose()}><form className="modal risk-modal" onSubmit={submit}><div className="modal-head"><div><h2>Tạo đợt đánh giá rủi ro</h2><p>Xác định phạm vi, trách nhiệm và chu kỳ rà soát trước khi ghi nhận rủi ro.</p></div><button type="button" className="icon-btn" onClick={onClose}><X size={20}/></button></div><div className="risk-form-grid"><label className="wide">Tên đợt đánh giá<input autoFocus required value={form.title} onChange={e => set('title', e.target.value)}/></label><label className="wide">Phạm vi đánh giá<textarea required rows={3} value={form.scope} onChange={e => set('scope', e.target.value)} placeholder="Hệ thống, tài sản, dịch vụ, dữ liệu và quy trình nằm trong phạm vi..."/></label><label>Chủ sở hữu<select required value={form.ownerId} onChange={e => set('ownerId', e.target.value)}><option value="">Chọn người chịu trách nhiệm</option>{operators.map(item => <option value={item.id} key={item.id}>{item.fullName}</option>)}</select></label><label>Người phê duyệt<select value={form.approverId} onChange={e => set('approverId', e.target.value)}><option value="">Chỉ định sau</option>{operators.filter(item => item.id !== form.ownerId).map(item => <option value={item.id} key={item.id}>{item.fullName}</option>)}</select></label><label>Phòng ban / phạm vi<select value={form.departmentId} onChange={e => set('departmentId', e.target.value)}><option value="">Toàn công ty</option>{departments.map(item => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label>Phương pháp<select value={form.methodology} onChange={e => set('methodology', e.target.value)}><option value="ISO_27005_NIST_800_30">ISO/IEC 27005 + NIST SP 800-30</option><option value="NIST_CSF_2">NIST CSF 2.0</option><option value="INTERNAL">Khung nội bộ</option></select></label><label>Ngày bắt đầu<input type="date" required value={form.startDate} onChange={e => set('startDate', e.target.value)}/></label><label>Ngày hoàn thành dự kiến<input type="date" value={form.targetDate} onChange={e => set('targetDate', e.target.value)}/></label><label>Ngày rà soát tiếp theo<input type="date" value={form.nextReviewAt} onChange={e => set('nextReviewAt', e.target.value)}/></label><label>Mô tả<textarea rows={2} value={form.description} onChange={e => set('description', e.target.value)}/></label>{error && <div className="risk-form-error wide">{error}</div>}</div><div className="modal-actions"><button type="button" className="btn secondary" onClick={onClose}>Hủy</button><button className="btn primary" disabled={saving}><Check size={16}/>{saving ? 'Đang lưu…' : 'Tạo đợt đánh giá'}</button></div></form></div>
}

function RiskModal({ assessments, operators, departments, assets, onClose, onSave }: { assessments: Assessment[]; operators: Lookup[]; departments: Lookup[]; assets: Asset[]; onClose: () => void; onSave: (body: any) => Promise<void> }) {
  const [form, setForm] = useState<any>({ assessmentId: assessments[0]?.id || '', title: '', category: 'An toàn thông tin', scenario: '', threat: '', vulnerability: '', existingControls: '', likelihood: 3, impact: 3, residualLikelihood: '', residualImpact: '', treatmentStrategy: 'MITIGATE', acceptanceRationale: '', ownerId: operators[0]?.id || '', departmentId: '', dueDate: '', nextReviewAt: '', assetId: '' }), [error, setError] = useState(''), [saving, setSaving] = useState(false)
  const set = (key: string, value: unknown) => setForm((current: any) => ({ ...current, [key]: value }))
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(''); try { await onSave({ ...form, likelihood: Number(form.likelihood), impact: Number(form.impact), residualLikelihood: form.residualLikelihood ? Number(form.residualLikelihood) : undefined, residualImpact: form.residualImpact ? Number(form.residualImpact) : undefined, acceptanceRationale: form.acceptanceRationale || undefined, departmentId: form.departmentId || undefined, dueDate: form.dueDate || undefined, nextReviewAt: form.nextReviewAt || undefined, assetIds: form.assetId ? [form.assetId] : [], incidentIds: [] }) } catch (reason) { setError(reason instanceof Error ? reason.message : 'Không thể ghi nhận rủi ro') } finally { setSaving(false) } }
  const inherent = Number(form.likelihood) * Number(form.impact), residual = form.residualLikelihood && form.residualImpact ? Number(form.residualLikelihood) * Number(form.residualImpact) : 0
  return <div className="modal-backdrop" onMouseDown={event => event.target === event.currentTarget && onClose()}><form className="modal risk-modal risk-item-modal" onSubmit={submit}><div className="modal-head"><div><h2>Ghi nhận rủi ro CNTT</h2><p>Mô tả theo kịch bản: tài sản/phạm vi – mối đe dọa – điểm yếu – ảnh hưởng.</p></div><button type="button" className="icon-btn" onClick={onClose}><X size={20}/></button></div><div className="risk-form-grid"><label>Đợt đánh giá<select required value={form.assessmentId} onChange={e => set('assessmentId', e.target.value)}>{assessments.filter(item => !['CLOSED', 'CANCELLED'].includes(item.status)).map(item => <option value={item.id} key={item.id}>{item.assessmentNo} · {item.title}</option>)}</select></label><label>Nhóm rủi ro<select value={form.category} onChange={e => set('category', e.target.value)}>{['An toàn thông tin', 'Hạ tầng mạng', 'Phần cứng', 'Phần mềm', 'Dịch vụ số', 'Dữ liệu', 'Nhà cung cấp', 'Liên tục kinh doanh', 'Tuân thủ', 'Khác'].map(item => <option key={item}>{item}</option>)}</select></label><label className="wide">Tiêu đề rủi ro<input autoFocus required value={form.title} onChange={e => set('title', e.target.value)}/></label><label className="wide">Kịch bản rủi ro<textarea required rows={2} value={form.scenario} onChange={e => set('scenario', e.target.value)} placeholder="Điều gì có thể xảy ra và gây ra kết quả nào?"/></label><label>Mối đe dọa<textarea required rows={2} value={form.threat} onChange={e => set('threat', e.target.value)}/></label><label>Điểm yếu / lỗ hổng<textarea required rows={2} value={form.vulnerability} onChange={e => set('vulnerability', e.target.value)}/></label><label className="wide">Biện pháp kiểm soát hiện có<textarea rows={2} value={form.existingControls} onChange={e => set('existingControls', e.target.value)}/></label><h3 className="wide">Đánh giá rủi ro vốn có</h3><label>Xác suất (1–5)<select value={form.likelihood} onChange={e => set('likelihood', e.target.value)}>{[1,2,3,4,5].map(item => <option key={item}>{item}</option>)}</select></label><label>Ảnh hưởng (1–5)<select value={form.impact} onChange={e => set('impact', e.target.value)}>{[1,2,3,4,5].map(item => <option key={item}>{item}</option>)}</select></label><div className={`risk-score-preview ${scoreLevel(inherent).toLowerCase()}`}><span>Rủi ro vốn có</span><b>{inherent} · {levelLabels[scoreLevel(inherent)]}</b></div><label>Chiến lược<select value={form.treatmentStrategy} onChange={e => set('treatmentStrategy', e.target.value)}>{Object.entries(strategyLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label><h3 className="wide">Rủi ro còn lại sau kiểm soát</h3><label>Xác suất còn lại<select value={form.residualLikelihood} onChange={e => set('residualLikelihood', e.target.value)}><option value="">Chưa đánh giá</option>{[1,2,3,4,5].map(item => <option key={item}>{item}</option>)}</select></label><label>Ảnh hưởng còn lại<select value={form.residualImpact} onChange={e => set('residualImpact', e.target.value)}><option value="">Chưa đánh giá</option>{[1,2,3,4,5].map(item => <option key={item}>{item}</option>)}</select></label><div className={`risk-score-preview ${residual ? scoreLevel(residual).toLowerCase() : ''}`}><span>Rủi ro còn lại</span><b>{residual ? `${residual} · ${levelLabels[scoreLevel(residual)]}` : 'Chưa đánh giá'}</b></div><label>Chủ sở hữu<select required value={form.ownerId} onChange={e => set('ownerId', e.target.value)}>{operators.map(item => <option value={item.id} key={item.id}>{item.fullName}</option>)}</select></label><label>Phòng ban<select value={form.departmentId} onChange={e => set('departmentId', e.target.value)}><option value="">Toàn công ty</option>{departments.map(item => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label>Tài sản liên quan<select value={form.assetId} onChange={e => set('assetId', e.target.value)}><option value="">Không gắn tài sản cụ thể</option>{assets.filter(item => item.apiId).map(item => <option value={item.apiId} key={item.apiId}>{item.code} · {item.name}</option>)}</select></label><label>Hạn xử lý<input type="date" value={form.dueDate} onChange={e => set('dueDate', e.target.value)}/></label>{form.treatmentStrategy === 'ACCEPT' && <label className="wide">Lý do chấp nhận<textarea required rows={2} value={form.acceptanceRationale} onChange={e => set('acceptanceRationale', e.target.value)}/></label>}{error && <div className="risk-form-error wide">{error}</div>}</div><div className="modal-actions"><button type="button" className="btn secondary" onClick={onClose}>Hủy</button><button className="btn primary" disabled={saving}><Plus size={16}/>{saving ? 'Đang lưu…' : 'Ghi nhận rủi ro'}</button></div></form></div>
}

function RiskDetail({ risk, operators, onClose, addControl, addTreatment, completeTreatment }: { risk: Risk; operators: Lookup[]; onClose: () => void; addControl: (id: string, body: any) => Promise<void>; addTreatment: (id: string, body: any) => Promise<void>; completeTreatment: (id: string, treatment: Treatment) => Promise<void> }) {
  const [control, setControl] = useState({ controlCode: '', title: '', framework: 'ISO/IEC 27001', status: 'PLANNED' }), [treatment, setTreatment] = useState({ title: '', assigneeId: operators[0]?.id || '', dueDate: '' }), [message, setMessage] = useState('')
  const saveControl = async () => { if (!control.title.trim()) return; try { await addControl(risk.id, control); setControl({ controlCode: '', title: '', framework: 'ISO/IEC 27001', status: 'PLANNED' }); setMessage('Đã bổ sung biện pháp kiểm soát.') } catch (error) { setMessage(apiMessage(error)) } }
  const saveTreatment = async () => { if (!treatment.title.trim() || !treatment.assigneeId || !treatment.dueDate) return; try { await addTreatment(risk.id, treatment); setTreatment({ title: '', assigneeId: operators[0]?.id || '', dueDate: '' }); setMessage('Đã giao hành động xử lý.') } catch (error) { setMessage(apiMessage(error)) } }
  return <div className="risk-detail-backdrop" onMouseDown={event => event.target === event.currentTarget && onClose()}><aside className="risk-detail"><header><div><span className={`risk-level ${risk.inherentLevel.toLowerCase()}`}>{risk.inherentScore} · {levelLabels[risk.inherentLevel]}</span><small>{risk.riskNo} · {risk.assessment.assessmentNo}</small><h2>{risk.title}</h2><p>{risk.category} · Chủ sở hữu: {risk.owner.fullName}</p></div><button className="icon-btn" onClick={onClose}><X size={20}/></button></header><div className="risk-detail-body"><section><h3>Kịch bản và phạm vi</h3><dl><div><dt>Trạng thái</dt><dd>{statusLabels[risk.status]}</dd></div><div><dt>Chiến lược</dt><dd>{strategyLabels[risk.treatmentStrategy]}</dd></div><div><dt>Hạn xử lý</dt><dd>{date(risk.dueDate)}</dd></div><div><dt>Rà soát tiếp theo</dt><dd>{date(risk.nextReviewAt)}</dd></div></dl><article><b>Kịch bản rủi ro</b><p>{risk.scenario}</p></article><article><b>Mối đe dọa</b><p>{risk.threat}</p></article><article><b>Điểm yếu / lỗ hổng</b><p>{risk.vulnerability}</p></article><article><b>Kiểm soát hiện có</b><p>{risk.existingControls || 'Chưa ghi nhận'}</p></article>{risk.assets.length > 0 && <article><b>Tài sản liên quan</b>{risk.assets.map(item => <p key={item.asset.id}>{item.asset.assetTag} · {item.asset.name}</p>)}</article>}</section><section className="risk-score-detail"><h3>Đánh giá rủi ro</h3><div><span><small>Vốn có</small><b className={risk.inherentLevel.toLowerCase()}>{risk.inherentScore}</b><em>Xác suất {risk.likelihood} × Ảnh hưởng {risk.impact}</em></span><ChevronRight/><span><small>Còn lại</small><b className={(risk.residualLevel || 'LOW').toLowerCase()}>{risk.residualScore || '—'}</b><em>{risk.residualScore ? `Xác suất ${risk.residualLikelihood} × Ảnh hưởng ${risk.residualImpact}` : 'Chưa đánh giá'}</em></span></div><h3>Biện pháp kiểm soát</h3>{risk.controls.map(item => <article key={item.id}><b>{item.controlCode && `${item.controlCode} · `}{item.title}</b><span>{item.framework || 'Nội bộ'} · {item.status}{item.effectiveness !== undefined && ` · Hiệu lực ${item.effectiveness}%`}</span></article>)}<div className="risk-inline-form"><input value={control.controlCode} onChange={e => setControl({ ...control, controlCode: e.target.value })} placeholder="Mã kiểm soát"/><input value={control.title} onChange={e => setControl({ ...control, title: e.target.value })} placeholder="Tên biện pháp kiểm soát"/><button className="btn secondary" onClick={() => void saveControl()}>Thêm kiểm soát</button></div></section><section><h3>Kế hoạch xử lý</h3>{risk.treatments.map(item => <article className="risk-treatment" key={item.id}><div><b>{item.title}</b><span>{item.assignee.fullName} · Hạn {date(item.dueDate)}</span><progress max="100" value={item.progress}/></div><strong>{item.progress}%</strong>{item.status !== 'COMPLETED' && <button className="btn secondary" onClick={() => void completeTreatment(risk.id, item)}>Hoàn tất</button>}</article>)}<div className="risk-treatment-form"><input value={treatment.title} onChange={e => setTreatment({ ...treatment, title: e.target.value })} placeholder="Hành động giảm thiểu / xử lý"/><select value={treatment.assigneeId} onChange={e => setTreatment({ ...treatment, assigneeId: e.target.value })}><option value="">Chọn người xử lý</option>{operators.map(item => <option value={item.id} key={item.id}>{item.fullName}</option>)}</select><input type="date" value={treatment.dueDate} onChange={e => setTreatment({ ...treatment, dueDate: e.target.value })}/><button className="btn primary" onClick={() => void saveTreatment()}><Plus size={15}/>Giao xử lý</button></div>{message && <p className="risk-detail-message">{message}</p>}</section></div></aside></div>
}
